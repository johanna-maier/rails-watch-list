<h1>Your movie + list selection and comment</h1>

<%= simple_form_for [ @list, @bookmark ] do |f| %>
  <%= f.association :movie %>
  <%= f.association :list %>
  <%= f.input :comment %>
  <%= f.button :submit %>
<% end %>
